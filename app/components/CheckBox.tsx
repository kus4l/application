export default function CheckBox() {
  return (
    <div>
      <input
        type="checkbox"
        className="checkbox checkbox-xs checkbox-success"
        disabled
        defaultChecked
      />{" "}
      Sign the application with an electronic signature and submit online.
    </div>
  );
}
